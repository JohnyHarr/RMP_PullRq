<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/buttons_color</item>
        <item name="colorPrimaryVariant">@color/buttons_color</item>
        <item name="colorOnPrimary">@color/buttons_color</item>
        <!-- Secondary brand color. -->
        <item name="colorOnSecondary">@color/buttons_color
        </item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>
        <item name="actionMenuTextColor">@color/text_color_but</item>
        <!-- Customize your theme here. -->
    </style>
</resources>